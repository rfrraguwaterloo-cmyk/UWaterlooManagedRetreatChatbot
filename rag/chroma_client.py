"""
chroma_client.py — stub kept for import compatibility.
ChromaDB was replaced by numpy cosine similarity in retriever.py
because chromadb is broken on Python 3.13 (HuggingFace Spaces runtime).
"""
